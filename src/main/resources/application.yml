spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:hospital_analytics}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:123456}
    hikari:
      jdbc-url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:hospital_analytics}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

    open-in-view: false
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 9090
coze:
  api:
    url: ${COZE_API_URL:http://localhost:8000/api/coze/chat}
    key: ${COZE_API_KEY:}
  workflow:
    id: ${COZE_WORKFLOW_ID:}

security:
  jwt:
    secret: ${JWT_SECRET:change-me-to-a-256-bit-secret-string}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:60}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174,http://localhost:4173,http://127.0.0.1:4173}
  websocket:
    allowed-origins: ${WS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174,http://localhost:4173,http://127.0.0.1:4173}
  init:
    admin:
      enabled: ${INIT_ADMIN_ENABLED:true}
      email: ${INIT_ADMIN_EMAIL:admin@hospital.local}
      password: ${INIT_ADMIN_PASSWORD:Admin123!}
      full-name: ${INIT_ADMIN_FULL_NAME:System Admin}
  analytics:
    enabled: ${ANALYTICS_ENABLED:false}
    datasource:
      url: jdbc:postgresql://${ANALYTICS_DB_HOST:localhost}:${ANALYTICS_DB_PORT:5432}/${ANALYTICS_DB_NAME:hospital_analytics}
      username: ${ANALYTICS_DB_USER:postgres}
      password: ${ANALYTICS_DB_PASSWORD:123456}
      hikari:
        jdbc-url: jdbc:postgresql://${ANALYTICS_DB_HOST:localhost}:${ANALYTICS_DB_PORT:5432}/${ANALYTICS_DB_NAME:hospital_analytics}
    refresh:
      daily-cron: ${ANALYTICS_DAILY_CRON:0 10 0 * * *}
      monthly-cron: ${ANALYTICS_MONTHLY_CRON:0 20 0 1 * *}

logging:
  level:
    org.springframework.security: INFO
    org.springframework.web: INFO
